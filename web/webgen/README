These are webgen sources for a site (http://webgen.rubyforge.org).

To use:
* First install Ruby and webgen
  (see http://webgen.rubyforge.org/download.html, I installed with Gems)
* To create the site, run 'webgen' in this directory
* More options with 'webgen --help'

That's it!
